version: '3.8'

services:
    backend:
        container_name: p2pix_block_explorer
        build:
            context: .
        ports:
            - "8000:8000"
        volumes:
            - .:/app
            - /app/node_modules
